name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: |
          echo "my super secret: ${{ secrets.SECRET }}"
          echo "my super secret: ${{ secrets.SECRET }}" > output.txt
          ls -l output.txt
          cat output.txt
